{
  "name": "polly-core",
  "version": "0.0.1",
  "description": "yet another entity component system",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_ENV=development webpack --progress --watch --color --config webpack.config.js",
    "build": "NODE_ENV=production webpack --progress --color --config webpack.config.js",
    "test": "NODE_ENV=test ../../node_modules/.bin/mocha --require babel-register --colors --check-leaks ./test/*.spec.js",
    "test-watching": "NODE_ENV=test ../../node_modules/.bin/mocha --require babel-register --colors --check-leaks -w ./test/*.spec.js",
    "test-coverage": "NODE_ENV=test ../../node_modules/.bin/istanbul cover ../../node_modules/.bin/_mocha ./test/*.spec.js -- --require babel-register"
  },
  "keywords": [
    "ecs",
    "entity",
    "component",
    "system",
    "game"
  ],
  "author": "Martin Emmert",
  "license": "ISC"
}
